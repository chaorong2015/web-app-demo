import * as React from 'react';
import { LoadingPageProps } from '../LoadingPage';

export default function LoadingPage(props: LoadingPageProps) {
  return (
    <div className="loading-page">正在加载中...</div>
  );
}
